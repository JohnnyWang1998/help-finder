# Required for ingest (GitHub API rate limits)
GITHUB_TOKEN=

# Optional — README summarization via Groq; falls back to README parsing if unset
GROQ_API_KEY=

# Required for Discord bot (Phase 3)
DISCORD_BOT_TOKEN=

# Base URL for dashboard links in bot replies
DASHBOARD_BASE_URL=http://localhost:3000
